# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

use_frameworks!

target 'OhSwifter' do
  pod 'SwiftMinions'
  target 'OhSwifterTests' do
    # pod 'SwiftMinions'x
  end

end
